# 项目必要依赖列表

# Web框架与服务器
fastapi==0.115.12  # FastAPI框架，用于构建Web API
uvicorn==0.34.1    # ASGI服务器，用于运行FastAPI应用

# 图像处理
Pillow==11.2.1     # 图像处理库，用于表情包绘制

# 以下是FastAPI的核心依赖项，确保功能正常运行
starlette==0.46.2  # FastAPI的基础库，提供异步支持
pydantic==2.11.3   # 数据验证库，FastAPI的核心依赖
annotated-types==0.7.0  # Pydantic的依赖
pydantic_core==2.33.1   # Pydantic的核心组件
anyio==4.9.0       # 异步I/O库，FastAPI使用

h11==0.14.0        # HTTP/1.1协议实现，Uvicorn的依赖
click==8.1.8       # 命令行接口工具，Uvicorn使用
